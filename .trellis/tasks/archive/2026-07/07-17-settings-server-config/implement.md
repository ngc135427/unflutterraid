# Implement: Settings server config

1. SettingsPageArgs + open from shell with client
2. ServerConfigPage form + reconnect/disconnect
3. l10n + main route
4. README + tests if pure helpers
5. analyze/test
